@page "/profile"
@attribute [Authorize(Policy = "IsCustomer")]
@inject HttpClient Http

<PageTitle>Profile</PageTitle>

<AuthorizeView>
    <Authorized>
        Hello, @context.User.Identity?.Name!
    </Authorized>
</AuthorizeView>

@code {
    // Using the http, grab all the info about the user and display it here and offer to view bookings
}
