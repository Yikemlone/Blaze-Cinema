@using Cinema.Shared.DTO
@inject HttpClient Http
@inject ISnackbar Snackbar

<MudDialog>
    <DialogContent>
        <p>Are you sure you want to decommission room <b>@Room.ID</b></p>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Close">Decommission</MudButton>
        <MudButton Color="Color.Secondary" OnClick="Cancel">Cancel</MudButton>
    </DialogActions>
</MudDialog>


@code {
    private const string API_URL = "api/manager";

    [CascadingParameter] MudDialogInstance MudDialog { get; set; }
    [Parameter] public RoomDTO Room { get; set; }
    [Parameter] public MudTable<RoomDTO> MudTable { get; set; }

    public async Task Close()
    {

        MudDialog.Close(DialogResult.Ok(true));
    }

    void Cancel()
    {
        MudDialog.Close(DialogResult.Cancel());
    }

}

